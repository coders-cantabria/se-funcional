## Recursion

```js
function fibonacci(n) {
    const go = (x, xx, pos) =>
        pos < 1
            ? x
            : go(x + xx, x, pos - 1)
    }

    return (
        n < 1
            ? 0
            : go(1, 0, n - 1)
    )
}
```
