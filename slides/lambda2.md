```js
const fibo_ = memo(
//  | ---------------------- λ ------------------------- |
    (n) => n < 1 ? 0 : n < 3 ? 1 : fibo_(n-1) + fibo_(n-2)
)

//           | ----------------- λ ------------------- |
$('a').click(function(e) { $(this).addClass('clicked') })

    // Patrón de módulo
    // IIFE / IIλ
    // λ()
;(function() {
    const exports = {}
    // exports.someFunction = ...
}())
```
